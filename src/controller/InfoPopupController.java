package controller;

public class InfoPopupController extends AbstractController  {

    //Action for the close button. This method closes the information pop up by calling the method in abstract controller
    public void close() {
        closePopup();
    }
}
